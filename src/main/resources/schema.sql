CREATE TABLE micronaut.json_sample(
                       id int auto_increment primary key,
                       name varchar(255),
                       properties json,
                       advanced json
);